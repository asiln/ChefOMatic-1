import { StyleSheet, Text, View } from 'react-native'
export const globalStyles = StyleSheet.create({
    titleText:{
        fontFamily: 'Inter',
        fontSize:20,
        color: "#333",
        fontWeight: 'bold'
    }
})